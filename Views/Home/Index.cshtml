@* <!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"] - Your Webpage Title</title>

    <style>
        /* Reset default margin and padding */
        body, html {
            margin: 0;
            padding: 0;
        }

        /* Set the background image and adjust its properties */
        body {
            background-image: url("../../wwwroot/images/Nike-Wallpaper-Desktop-4k-2048x1280.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            height: 100vh; /* Ensure the background covers the entire viewport height */
        }

        /* Add your custom styles for other content if needed */
        /* For example: */
        .content {
            text-align: center;
            padding: 20px;
            color: white;
        }
    </style>
</head>

<body>
   
        <div class="brand-name">VANKIDAS</div>
        <div class="slogan">Discover the Best Products</div>
       
    
   
</body>

</html>

 *@

<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"] - Your Webpage Title</title>

    <style>
        /* Reset default margin and padding */
        body, html {
            margin: 0;
            padding: 0;
        }

        /* Set the background image and adjust its properties */
        body {
            background-image: url("/images/shoe.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            height: 100vh; /* Ensure the background covers the entire viewport height */
            position: relative; /* Create a stacking context for the overlay */
        }

        /* Add the overlay */
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.3); /* Darken the background */
            z-index: -1; /* Place the overlay behind other content */
        }

        /* Add styles for the content */
        .content {
            text-align: center;
            padding: 20px;
            color: white;
            z-index: 1; /* Place the content above the overlay */
        }

        /* Add styles for the brand name */
        .brand-name {
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 10px;
            color: #ccc;
        }

        /* Add styles for the slogan */
        .slogan {
            font-size: 1.5rem;
            color: #ccc; /* Gray color */
        }
    </style>
</head>

<body>
    <div class="overlay"></div> <!-- Overlay -->
    <div class="content">
        <div class="brand-name">VANKIDAS</div>

        <div class="slogan">Discover the Best Products</div>
    </div>
</body>

</html>
